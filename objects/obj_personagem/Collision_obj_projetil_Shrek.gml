/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
if alarm[0] <= 0{
	vida -= 1;
	alarm[0] = inv_tempo; }//3segundos sem dano
	
	with(other){
	
	instance_destroy();
	
	}